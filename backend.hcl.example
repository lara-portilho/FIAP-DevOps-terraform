bucket  = "SEU_BUCKET_DE_STATE"
key     = "togglemaster/terraform.tfstate"
region  = "us-east-1"
encrypt = true

# Lock nativo do S3 (requer Terraform >= 1.10). Descomente para habilitar:
# use_lockfile = true
